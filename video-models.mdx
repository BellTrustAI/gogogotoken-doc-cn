---
title: "视频模型列表"
sidebarTitle: "模型列表"
description: "Seedance 2.0 视频生成模型与 CNY 计费方式概览"
icon: "list-video"
---

Seedance 2.0 视频生成接口提供 ARK 原生模型，按**输出 token** 据实计费，支持 480p、720p、1080p 分辨率（mini 仅 480p / 720p）。

<Note>
  本页仅覆盖 **Seedance（豆包）** 模型。可灵 **Kling** 视频（积分计费、`kling-v2-*` / `kling-v3`）见 [Kling 模型与计费](/kling-models)。
</Note>

<Note>
  费率对齐 **火山引擎 ARK 官方人民币价目**（2026-06），与海外站（gogogotoken.ai）美元价目**独立维护**，不是汇率换算。
</Note>

## 可用模型

| 模型 | 说明 | 最高分辨率 | 媒体输入 |
| --- | --- | --- | --- |
| `doubao-seedance-2-0` | ARK 原生旗舰模型，兼容 OpenAI Video API，任务完成后自动差额结算 | 1080p | 支持 |
| `doubao-seedance-2-0-fast` | 更快、成本更低 | 720p | 支持 |
| `doubao-seedance-2-0-mini` | 轻量版，适合低成本预览与批量试片 | 720p | 支持 |

## 官方单价（CNY / 百万 output tokens）

### `doubao-seedance-2-0`

| 分辨率 | 纯文本 / 文生（无媒体输入） | 含媒体输入（图生 / 参考 / `asset://` 等） |
| --- | --- | --- |
| 480p / 720p | ¥46 | ¥28 |
| 1080p | ¥51 | ¥31 |

### `doubao-seedance-2-0-fast`

| 场景 | CNY / 百万 tokens |
| --- | --- |
| 纯文本 / 文生（无媒体输入） | ¥37 |
| 含媒体输入 | ¥22 |

### `doubao-seedance-2-0-mini`

轻量模型**不支持 1080p**，传 `1080p` 或 `1080x1920` 会被服务端拒绝。

| 场景 | CNY / 百万 tokens |
| --- | --- |
| 纯文本 / 文生（无媒体输入） | ¥23 |
| 含媒体输入 | ¥14 |

<Note>
  **媒体输入**包括：`images[]`、公网 `image_url` / `video_url` / `audio_url`，以及 `metadata.content[]` 中的 `asset://` 首帧或参考素材。不仅限于「视频 URL」字段。
</Note>

## 分辨率

可通过 `size` 或 `metadata.resolution` 指定输出分辨率。

```json
{
  "size": "720x1280",
  "metadata": {
    "resolution": "720p"
  }
}
```

<Note>
  分辨率优先级：`metadata.resolution` → `size`（转为 `480p`/`720p`/`1080p`）→ 默认 `720p`。`doubao-seedance-2-0-fast` 与 `doubao-seedance-2-0-mini` 传 `1080p` 会被拒绝。
</Note>

## 任务模式

| 模式 | 接口 | 说明 |
| --- | --- | --- |
| 文生视频 | `POST /v1/video/generations` | 仅传提示词生成视频 |
| 图生视频 | `POST /v1/video/generations` | 传入首帧图片、参考图片或 `metadata.content[]` |
| 任务查询 | `GET /v1/video/generations/{task_id}` | 轮询查询任务状态和结果 |

## 相关页面

- [视频生成](/video-generations) — 提交文生 / 图生任务
- [Token 计费](/seedance-token) — 参数与 `completion_tokens` 结算
- [素材管理](/assets) — 上传并在图生中引用 `asset://`
