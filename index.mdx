---
title: "GogoToken — 面向开发者的 OpenAI 兼容 AI API 聚合中转平台，Claude 与 GPT 按量接入"
sidebarTitle: "首页"
description: "GogoToken 是面向开发者的 OpenAI 兼容 AI API 聚合平台，支持 Claude Opus 4.8、Claude Sonnet 4.6、GPT-5.5、GPT-4o，按量计费无月费无订阅，修改一行 baseURL 即可接入所有顶尖大模型，充值即用。"
---

GogoToken 是专为极客打造的 AI 燃料补给站。只需将 `baseURL` 改为 `https://gogotoken.org/v1`，即可通过任何 OpenAI 兼容的 SDK 访问 Claude Opus 4.8、GPT-5.5 等顶尖模型，无需订阅，用多少充多少。

<CardGroup cols={2}>
  <Card title="快速开始" icon="rocket" href="/quickstart">
    5 分钟内完成注册并发送你的第一个 AI 请求
  </Card>
  <Card title="API 密钥" icon="key" href="/account/api-keys">
    创建并管理用于接入的 API Key
  </Card>
  <Card title="支持的模型" icon="microchip" href="/concepts/models">
    查看当前在线的所有 AI 模型及能力说明
  </Card>
  <Card title="API 参考" icon="code" href="/api/overview">
    完整的接口文档、参数说明与示例响应
  </Card>
</CardGroup>

## 一行代码接入

```javascript quickstart.js
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'your-api-key',      // 替换为你的 GogoToken API Key
  baseURL: 'https://gogotoken.org/v1',  // 唯一需要改动的地方
});

const completion = await client.chat.completions.create({
  model: 'claude-opus-4-8',
  messages: [{ role: 'user', content: '你好，介绍一下你自己' }],
});

console.log(completion.choices[0].message.content);
```

## 为什么选择 GogoToken

<CardGroup cols={2}>
  <Card title="按量计费" icon="coins">
    无月费、无订阅。充值余额，用多少扣多少，余额可退。
  </Card>
  <Card title="OpenAI 兼容" icon="plug">
    完全兼容 OpenAI SDK，只需修改 `baseURL`，无需改动业务代码。
  </Card>
  <Card title="稳定优先" icon="shield-check">
    已上线模型保证高可用，稳定性高于价格竞争。
  </Card>
  <Card title="顶尖模型" icon="stars">
    Claude Opus 4.8 旗舰、GPT-5.5 多模态，持续接入最新模型。
  </Card>
</CardGroup>

<Steps>
  <Step title="注册账号">
    前往 [gogotoken.org/register](https://gogotoken.org/register) 完成注册（邀请制，按节奏开放）。
  </Step>
  <Step title="充值余额">
    登录控制台，按需充值。余额透明可查，随时可申请退款。
  </Step>
  <Step title="创建 API Key">
    在控制台的 API 密钥页面生成你的专属 Key，妥善保管。
  </Step>
  <Step title="开始调用">
    将 `baseURL` 设置为 `https://gogotoken.org/v1`，使用任意 OpenAI SDK 发起调用。
  </Step>
</Steps>
